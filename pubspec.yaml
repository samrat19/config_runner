name: config_runner
description: "A powerful Dart command-line tool that streamlines Flutter project setup by automating the creation of essential configuration and feature module boilerplate. It generates environment-specific configuration files and scaffolding with your modules"
version: 0.0.4
homepage: https://github.com/samrat19/config_runner
repository: https://github.com/samrat19/config_runner

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  yaml: ^3.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

executables:
  config:
